(() => {
const hero: string = 'Flash';

function returnName(): string {
    return hero;
}

const activableSuignal = (): string => {
    return 'Signal activated!';
}

const heroname = returnName();

})();